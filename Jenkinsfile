pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo from shell'
                sh 'whoami'
            }
        }
    }
}
